<header class="site-header{% if section.settings.sticky %} site-header--sticky{% endif %}">
  <div class="site-header__inner page-width">
    <a class="brand" href="{{ routes.root_url }}" aria-label="{{ shop.name }}">
      {%- if settings.logo != blank -%}
        {{ settings.logo | image_url: width: 680 | image_tag: widths: '240, 360, 520, 680', width: settings.logo_width, loading: 'eager', alt: shop.name }}
      {%- else -%}
        <img src="{{ 'rascals-logo.png' | asset_url }}" width="{{ settings.logo_width }}" height="120" alt="{{ shop.name }}">
      {%- endif -%}
    </a>

    <nav class="desktop-nav" aria-label="Hauptnavigation">
      <ul>
        {%- for link in section.settings.menu.links -%}
          <li>
            <a href="{{ link.url }}"{% if link.current %} aria-current="page"{% endif %}>{{ link.title | escape }}</a>
          </li>
        {%- endfor -%}
      </ul>
    </nav>

    <div class="header-actions">
      <a class="header-action" href="{{ routes.search_url }}" aria-label="{{ 'general.search.search' | t }}">⌕</a>
      <a class="header-action header-cart" href="{{ routes.cart_url }}" aria-label="{{ 'cart.title' | t }}">
        <span>▱</span><b data-cart-count>{{ cart.item_count }}</b>
      </a>
      <details class="mobile-menu">
        <summary aria-label="Navigation öffnen"><span></span><span></span><span></span></summary>
        <nav aria-label="Mobile Navigation">
          {%- for link in section.settings.menu.links -%}<a href="{{ link.url }}">{{ link.title | escape }}</a>{%- endfor -%}
        </nav>
      </details>
    </div>
  </div>
</header>

{% schema %}
{
  "name": "Header",
  "settings": [
    { "type": "link_list", "id": "menu", "label": "Main menu", "default": "main-menu" },
    { "type": "checkbox", "id": "sticky", "label": "Fixierten Header verwenden", "default": true }
  ]
}
{% endschema %}
